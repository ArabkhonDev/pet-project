import { BrowserRouter, Routes, Route } from "react-router-dom";

const Allroute = () => {
  return (
    <BrowserRouter>
      <Routes>
        <Route />
      </Routes>
    </BrowserRouter>
  );
};

export default Allroute;
